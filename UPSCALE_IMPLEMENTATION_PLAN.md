# План внедрения апскейла через Replicate.com

Этот план описывает шаги по добавлению функционала ультра-высокого разрешения (8K/12K/16K) для подготовки изображений к печати на фотообоях (3000x2000 мм).

## 📝 Список задач (To-Do List)

### 1. Инфраструктура и типы данных
- [ ] **Обновить `types.ts`**:
    - Добавить `8K`, `12K`, `16K` в перечисление `ImageSize`.
    - Добавить `UpscaleSettings` (формат: JPG/PNG, масштаб, целевое разрешение).
    - Расширить `GenerationSettings` настройками апскейла.
- [ ] **Обновить `constants.ts`**:
    - Добавить новые разрешения в `IMAGE_SIZES`.
    - Добавить конфигурацию для Replicate (ID модели `nightmare-ai/real-esrgan`).

### 2. Реализация сервиса
- [ ] **Создать `services/replicateService.ts`**:
    - Реализовать функцию отправки изображения на API Replicate.
    - Реализовать механизм поллинга (проверки статуса) до завершения обработки.
    - Обработка ошибок и таймаутов.

### 3. Пользовательский интерфейс (UX)
- [ ] **Header**: Добавить поле ввода для `Replicate Token` рядом с Gemini Key.
- [ ] **Шаг 1 (Настройки)**:
    - Добавить секцию "Параметры печати".
    - Переключатель активации апскейла.
    - Выбор итогового формата (JPG — рекомендуется, PNG — без потерь).
    - Информационный блок: расчет размера в метрах при 90 DPI.
- [ ] **Шаг 3 (Результат)**:
    - Двухэтапный индикатор прогресса (Генерация -> Апскейл).
    - Отображение финального разрешения и прямой ссылки на скачивание.

### 4. Интеграция логики
- [ ] **Цепочка вызовов в `App.tsx`**:
    - Если апскейл включен: `Gemini (4K base)` -> `Replicate (4x upscale)`.
    - Если только апскейл (загруженное фото без промпта): прямой вызов `Replicate`.
- [ ] **Обработка скачивания**: Реализовать корректное сохранение больших файлов из внешнего CDN.

## 🛠 Технические детали
- **Модель**: `real-esrgan` (лучшее решение для иллюстраций и чистых пейзажей).
- **Масштаб**: 4x (оптимально для перехода из 4K в 16K без потери четкости).
- **Цена**: ~$0.01 - $0.05 за изображение (оплата только за секунды работы GPU).
- **Формат**: Рекомендуется JPG (95-100% качество) для обоев, чтобы файл весил ~40МБ вместо 150МБ+.

---
*План подготовлен для выполнения в Act Mode.*
